aps: "0.1"
id: "dev.simple"
name: "Simple"
version: "0.0.1"
summary: "Echo upper."
runtimes:
  - kind: "python"
    entrypoint: ["python","-m","simple.main"]
capabilities:
  inputs:
    schema:
      type: object
      properties:
        text: {type: string}
      required: ["text"]
  outputs:
    schema:
      type: object
      properties:
        text: {type: string}
      required: ["text"]
policies:
  network: { egress: [] }
