aps_version: "0.1"
id: "dev.error"
name: "Error"
version: "0.0.1"
summary: "Echoes the input text."
runtimes:
  - kind: "python"
    entrypoint: ["python", "-m", "error.main"]
capabilities:
  inputs:
    schema:
      type: object
      properties:
        text: { type: string }
      required: ["text"]
  outputs:
    schema:
      type: object
      properties:
        text: { type: string }
      required: ["text"]
  tools: []
policies:
  network: { egress: [] }


