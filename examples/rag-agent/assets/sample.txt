APS is an open agent packaging standard to enable interoperability, distribution, and trust for AI agents.
Agents are packaged with a manifest (aps/agent.yaml) and a runtime entrypoint that reads JSON on stdin and returns JSON on stdout.
