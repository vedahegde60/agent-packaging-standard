# Getting Started with APS (working title)

This guide shows how to install the CLI, validate an agent, and run two examples (echo + RAG).

## 1) Install the CLI (editable)

```bash
cd cli
pip install -e .
